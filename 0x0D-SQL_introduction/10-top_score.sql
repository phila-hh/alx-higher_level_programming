-- Lists all records of the table second_table of the database hbtn_0c_0
-- Results display both score and name, ordered by the score
SELECT `score`, `name` FROM `second_table` ORDER BY `score` DESC;
